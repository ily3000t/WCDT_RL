# Local configuration

This directory is ignored except for documentation and `*.example.yaml` files.
Copy a template from `../examples/` here when a machine needs local checkpoint
paths. Local overlays are never canonical and must not feed formal evidence.
